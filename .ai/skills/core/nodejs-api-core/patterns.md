# nodejs-api-core Patterns

Load this skill only when its task trigger applies. Keep state transitions in the v2 control plane and use the skill for task-level judgment, not as an alternative workflow runtime.
