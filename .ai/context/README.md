# Context Engine

The Context Engine starts from the task's owned files, workflow state, assigned
role contract, and routed skills. `context-pack.sh T<n>` emits this deterministic
path list. Read wider project context only when the task evidence requires it.
