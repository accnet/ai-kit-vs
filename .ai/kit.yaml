kit:
  id: ai-kit-v2
  version: 2.0.0
  entrypoint: AGENTS.md
  work_dir: .ai-work
  skills_dir: .ai/skills
  registry: .ai/registry.yaml

project:
  stack: []
  source_dirs: []

verification:
  test_command: npm test
  typecheck_command: npm run check
  build_command: npm run build
  lint_command: npm run lint
